pipeline {
 agent any
 stages {
    stage('build') {
      steps {
	    echo "Running ${env.BUILD_ID} on ${env.WORKSPACE}"
        echo 'Building...'
      }
    }
    stage('test') {
      steps {
        echo 'testing...'
		
      }
    }
  }
}