pipeline {
 agent any
 environment {
    PATH = "C:/RF-Git/TestRepo"
  }
 stages {
    stage('build') {
      steps {
	    echo "Running ${env.BUILD_ID} on ${env.WORKSPACE} on ${env.PATH}"
        sh "ls -la ${env.WORKSPACE}"
      }
    }
    stage('test') {
      steps {
        echo 'testing...'
		
      }
    }
  }
}